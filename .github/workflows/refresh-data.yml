name: Refresh data

on:
  workflow_dispatch:

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npm run validate:backend
      - name: Placeholder until election ETL lands
        run: |
          echo "UK Elections ETL is not wired yet."
          echo "Source snapshot validation is wired. Add reviewed Democracy Club, local results, boundary, and polling ingestion before enabling a schedule."
