name: Refresh data

on:
  workflow_dispatch:

jobs:
  refresh:
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - name: Placeholder until election ETL lands
        run: |
          echo "UK Elections ETL is not wired yet."
          echo "Add Democracy Club / historic results ingestion before enabling a schedule."
